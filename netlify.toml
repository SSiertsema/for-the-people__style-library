[build]
  command = "npm run build:tokens && cd server && npm install && npm run generate"
  publish = "server/.output/public"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
